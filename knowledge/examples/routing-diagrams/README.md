# Routing Diagrams

Routing diagrams use plain text so MCP clients can consume them as context.

Diagram conventions:

- `->` means audio or video flows to a bus, output, or composed scene.
- `(exclude self)` marks mix-minus behavior.
- `[review]` marks assumptions that need operator confirmation.
